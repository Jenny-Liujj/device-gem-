<%= form_for(@userway) do |f| %>
  <% if @userway.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@userway.errors.count, "error") %> prohibited this userway from being saved:</h2>

      <ul>
      <% @userway.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
